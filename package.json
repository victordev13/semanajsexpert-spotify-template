{
  "name": "semana-js-expert",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "live-reload": "nodemon server/index.js",
    "test": "cross-env LOG_DISABLED=true NODE_OPTIONS=--experimental-vm-modules npx jest --runInBand --no-cache --no-warnings",
    "test:watch": "cross-env LOG_DISABLED=true NODE_OPTIONS=--experimental-vm-modules npx jest --runInBand --no-cache --no-warnings --watchAll",
    "test:coverage": "cross-env LOG_DISABLED=true NODE_OPTIONS=--experimental-vm-modules npx jest --runInBand --no-cache --no-warnings --coverage tests/unit",
    "live-reload:docker": "docker-compose up live-reload"
  },
  "engines": {
    "node": "17"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^27.5.1",
    "nodemon": "^2.0.15"
  },
  "dependencies": {
    "pino": "^7.8.0",
    "pino-pretty": "^7.5.3"
  }
}
